#include "main.h"

/**
 * main - tests function that printsif
 * Return: 0
 */

int main(void)
{
	int i;
	int i;

	i = 0;
	positive_or_negative(i);
	i = 0;
	positive_or_negative(i);

	return (0);
	return (0);
}
