#include "main.h"

/**
*_isupper - checks if uppercase
*@c: character to be checked
*Return: 1 if uppercase
*/

int _isupper(int c)
{
if ((c >= 'A') && (c <= 'Z'))
    {
          Return (1);
    }
return(0);
}
